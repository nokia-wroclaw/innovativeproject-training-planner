{
  "name": "trainingplaner",
  "scripts": {
    "server": "nodemon server/server.js",
    "client": "npm start --prefix client",
    "dep": "npm i && npm i --prefix client && npm i --prefix server",
    "dev": "npm run dep && concurrently \"npm run server\" \"npm run client\""
  },
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2"
  }
}
